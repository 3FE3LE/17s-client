# nine-cc-web — Local Agent Directives

> Auto-generated by `create-product.mjs` (per-app agent orchestration). Owner fills in TODO sections. This fence in particular has been enriched with Day-1 invariants — do NOT loosen them when filling in further sections.

## Own files

- `apps/nine-cc-web/**` — everything under this directory is owned by this app. `apps/nine-cc-mobile/**` is owned by its own app; coordinate UI changes that should mirror across both.

## Neighbor apps

- `nine-cc-mobile` — same product, same module, same auth surface; coordinate UI changes that should mirror across the dashboard.

## Branch scope

- `feat/9cc-*`, `fix/9cc-*` — feature/fix branches for nine-cc-web.
- Branches off `development` (per the 3-tier release workflow in `AGENTS.md`).

## Do not modify without confirmation

- `apps/nine-cc-web/proxy.ts` — Clerk auth bridge. Coordinate with auth-owner.
- `apps/nine-cc-web/vercel.mjs` — Vercel project wiring.
- `apps/nine-cc-web/pnpm-lock.yaml` — only via `pnpm install` in this app dir.
- `17s-client/packages/core/src/api-schema.ts` — generated; regenerate via `pnpm --filter @17suit/core gen:api-schema`, don't hand-edit.
- `17s-client/packages/modules/nine-care-companion/**` — shared domain logic with future surfaces (Alexa skill, mobile/PWA later). Coordinate cross-surface changes with the domain owner.

## Cross-cutting invariants (carry into every change)

These mirror `.claude/agents/nine-cc.md` (the **product-level** agent — every surface shares it) and are restated here so the working-tree operating contract is self-contained.

- **Backend = source of truth.** No state derives in the client. Reads hit the backend or a derived view it authorises. Writes always go through the backend.
- **Medication responses deterministic.** No LLM in the factual med-status request path.
- **Alexa never decides medical state.** (Out of this app's scope; mirror the rule across collaboration with the Alexa skill repo when it lands.)
- **Every missed critical medication escalates.** Per-care-circle configuration decides recipients and cadence.
- **Privacy > convenience.** Defaults favor minimal data exposure.

## Related Context

- **Product agent: `.claude/agents/nine-cc.md`** — single source of truth for product philosophy, invariants, compliance. Read first for cross-cutting product concerns.
- **Memory: `/home/fl/.claude/projects/-home-fl-17s-workspace/memory/project/nine-cc.md`** — non-derivable facts: ownership, compliance posture, integration partners, why-this-product.
- **Neighbor fence: `apps/nine-cc-mobile/AGENTS.md`** — same product, mobile conventions.
- **Registry entries: `apps-registry.json#apps.nine-cc-web` + `apps.nine-cc-mobile`** (both rows point at the same product agent and memory file).
- **Module: `17s-client/packages/modules/nine-care-companion`** — frontend-agnostic domain logic.

## Local conventions

- auth: `@clerk/*`.
- styling: `@17suit/ui-web` primitives + `@17suit/design-system` tokens.
- forms: `react-hook-form` + `zod` schemas in `@17suit/core`.
- i18n: target `@17suit/i18n` (not yet extracted — keep UI strings in `app/<locale>/` shape from Day 1 to make future extraction mechanical).
- observability: `@sentry/nextjs`.
- tests: mirror landing's vitest setup (the only suite wired today); extend as the app grows.

## Compliance posture (Day 1)

- GDPR default. Logs that touch medication state are redacted server-side before they hit any log sink — that contract belongs in the server, not the client, but never relax it from this side either.
- No third-party analytics on routes that render patient health state.

## Open (not blocking Day 1)

- Care Circle onboarding flow (Day 1 in MVP scope, not yet built).
- Daily timeline view (Day 1 in MVP scope, not yet built).
- Medication block confirmation flow.
- Escalation rule editor.
- Alexa skill account linking — separate repo, separate human, but architecturally coupled.
