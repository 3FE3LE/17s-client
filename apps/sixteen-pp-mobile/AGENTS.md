# sixteen-pp-mobile — Local Agent Directives

> Auto-generated by `create-product.mjs` (per-app agent orchestration). Owner fills in TODO sections.

## Own files

- `apps/sixteen-pp-mobile/**` — everything under this directory is owned by this app.

## Neighbor apps

- `sixteen-pp-web` — same product, same module, same auth surface; coordinate UI changes that should mirror.

## Branch scope

Shell only during the web-first MVP — no product-specific mobile features yet. Convention:

- `feat/sixteen-pp-*`, `fix/sixteen-pp-*`
- Allowed paths: `apps/sixteen-pp-mobile/**` plus directly related registry/agent/memory/doc files.
- Mobile must NOT drive architectural decisions during the web-first MVP.

## Do not modify without confirmation

- `apps/sixteen-pp-mobile/app.json` — Expo project config (slug, bundle id, scheme).
- `apps/sixteen-pp-mobile/pnpm-lock.yaml` — only via `pnpm install` in this app dir.
- `17s-client/packages/core/src/api-schema.ts` — generated; regenerate, don't hand-edit.
- `17s-client/packages/modules/sixteen-pixel-perfect/**` — shared with `sixteen-pp-web`; coordinate cross-app.

## Local conventions

- Expo / React Native shell inherited from the monorepo mobile template.
- Keep as a valid workspace shell; do not implement pixel-perfect product features until the web pipeline is proven.
- Shared domain lives in `@17suit/module-sixteen-pixel-perfect`; reuse it rather than reimplementing on mobile.
