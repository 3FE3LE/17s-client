# sixteen-pp-web — Local Agent Directives

> Auto-generated by `create-product.mjs` (per-app agent orchestration). Owner fills in TODO sections.

## Own files

- `apps/sixteen-pp-web/**` — everything under this directory is owned by this app.

## Neighbor apps

- `sixteen-pp-mobile` — same product, same module, same auth surface; coordinate UI changes that should mirror.

## Branch scope

Web-first MVP. Feature branches target only this app + the shared module. Convention:

- `feat/sixteen-pp-*`, `fix/sixteen-pp-*`
- Allowed paths: `apps/sixteen-pp-web/**`, `packages/modules/sixteen-pixel-perfect/**`, and directly related registry/agent/memory/doc files.
- Do NOT modify unrelated apps or shared packages unless required for successful compilation.

## Do not modify without confirmation

- `apps/sixteen-pp-web/proxy.ts` — auth bridge; coordinate with auth-owner.
- `apps/sixteen-pp-web/vercel.mjs` — Vercel project wiring.
- `apps/sixteen-pp-web/pnpm-lock.yaml` — only via `pnpm install` in this app dir.
- `17s-client/packages/core/src/api-schema.ts` — generated; regenerate, don't hand-edit.
- `17s-client/packages/modules/sixteen-pixel-perfect/**` — shared with `sixteen-pp-mobile`; coordinate cross-app.

## Local conventions

- React + TypeScript, Next.js app router (monorepo conventions).
- HTML Canvas for pixel rendering; keep rendering logic in pure module functions, out of large components.
- Serializable `AssetRecipe` objects; local browser persistence for save/load; export generated client-side.
- No backend/DB for the MVP. Auth is the inherited landing-origin Clerk shell only.
- Invariants (integer coords, integer zoom, smoothing off, deterministic recipe+seed, transparency, 9-slice corner integrity) are non-negotiable — see `.claude/agents/sixteen-pp.md`.
