import { StudioClient } from '@/components/studio/studio-client';

export const metadata = {
  title: 'Estudio | Sixteen Pixel Perfect',
  description: 'Generador determinista de assets de UI en pixel-art.',
};

export default function StudioPage() {
  return <StudioClient />;
}
