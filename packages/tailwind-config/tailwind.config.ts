import type { Config } from 'tailwindcss';

const config: Config = {
  content: [],
  theme: {
    extend: {
      colors: {
        brand: {
          DEFAULT: '#0E7490',
          foreground: '#F8FAFC',
        },
      },
    },
  },
  plugins: [],
};

export default config;
