Copy any existing `*-web`, `*-mobile`, and matching `packages/modules/*` package.
Then update:

1. package names (`@17suit/module-...`)
2. app display names/subdomains
3. domain schema and service functions
